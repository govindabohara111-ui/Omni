# OpenAI API key used by the agent engine (GPT-4o)
OPENAI_API_KEY=sk-...

# SQLite database location (relative to the prisma/ directory)
DATABASE_URL="file:./dev.db"

# Optional: override the Docker socket path (defaults to /var/run/docker.sock)
# DOCKER_SOCKET=/var/run/docker.sock
